---
name: Feature Implementation Ticket
about: One scope-bounded engineering ticket generated by /tickets (Plan -> Contracts -> Ticket)
title: "[ADHOC-02] Enhancement: Add 1-click Demo Account pre-fill on login page"
labels: ["enhancement", "product-playbook", "lanekeeper"]
---

### 🎯 Goal
Provide a 1-click "Use Demo Account" quick-fill button on the `/login` page during development so reviewers and testers can immediately authenticate without manually retyping credentials or encountering typos.

### 🧭 Slice Strategy
- [ ] ↕️ Vertical — thin end-to-end increment, demoable on merge
- [ ] ↔️ Horizontal — single architectural layer
- [x] 🐛 Ad-hoc — bug · edge-case · tech-debt · security

### 🛣️ Lane
auth

### 🧱 Layer(s)
- [ ] 📦 Storage / data provider — schema models, persistence adapter, migration
- [ ] ⚙️ Pure domain service — business logic, state transitions, validation
- [x] 🎨 UI component — isolated component, interactive states, a11y, `DESIGN.md` tokens
- [ ] 🧪 Cross-layer verification — integration across the real seam, adversarial/security

### 📁 Target Files
- `src/components/features/auth-form.tsx`
- `docs/features/auth.md`

### 🔌 Contract — Inputs → Outputs
- **Consumes:** Pre-seeded local credentials (`kishore@test.com`, `Password123!`)
- **Exposes:** Form input pre-fill action on the `AuthForm` UI

### 🔗 Depends On
`[ADHOC-01]`

### 👀 Demo — what works after this merges
When visiting `http://localhost:3000/login`, a subtle helper button "Use Demo Account" appears. Clicking it immediately populates `kishore@test.com` and `Password123!`, allowing instant 1-click sign-in into `/dashboard`.

### 🛠️ Implementation Tasks
- [ ] Add conditional helper affordance in `src/components/features/auth-form.tsx` for `mode === 'login'`.
- [ ] Pre-populate `email` and `password` states on click.
- [ ] Ensure button adheres to `DESIGN.md` tokens (`var(--secondary)`, `var(--secondary-foreground)`).
- [ ] Update `docs/features/auth.md`.

### 🔒 Security in the Definition of Done
- [x] Helper is enabled only for local development environments (`NODE_ENV !== 'production'`).
- [x] Passwords are never sent in URLs or plain query parameters.
- [x] Submission still goes through standard Argon2id/bcrypt verification in PostgreSQL.

### 🧪 Verification Plan
- **Automated verification:** `pnpm lint` && `pnpm test`
- **Manual verification:** Navigate to `http://localhost:3000/login`, click "Use Demo Account", verify fields populate, and click "Sign In".
