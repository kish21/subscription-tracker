---
name: Feature Implementation Ticket
about: One scope-bounded engineering ticket generated by /tickets (Plan -> Contracts -> Ticket)
title: "[ADHOC-01] Bug: React hydration mismatch caused by password manager browser extensions"
labels: ["bug", "product-playbook", "lanekeeper"]
---

### 🎯 Goal
Prevent React client hydration mismatch errors (`react-dom-client.development.js:5524 Uncaught Error: Hydration failed`) when password manager browser extensions (e.g., Surfshark, Bitwarden, 1Password) inject custom DOM elements (such as `<shark-icon-container>`) into form `<input>` wrappers before React client hydration completes.

### 🧭 Slice Strategy
- [ ] ↕️ Vertical — thin end-to-end increment, demoable on merge
- [ ] ↔️ Horizontal — single architectural layer
- [x] 🐛 Ad-hoc — bug · edge-case · tech-debt · security

### 🛣️ Lane
auth

### 🧱 Layer(s)
- [ ] 📦 Storage / data provider — schema models, persistence adapter, migration
- [ ] ⚙️ Pure domain service — business logic, state transitions, validation
- [x] 🎨 UI component — isolated component, interactive states, a11y, `DESIGN.md` tokens
- [x] 🧪 Cross-layer verification — integration across the real seam, adversarial/security

### 📁 Target Files
- `src/components/features/auth-form.tsx`
- `docs/features/auth.md`

### 🔌 Contract — Inputs → Outputs
- **Consumes:** Browser DOM state during initial SSR page load
- **Exposes:** Extension-resilient `AuthForm` component with zero hydration mismatch warnings

### 🔗 Depends On
`[M1-SLICE-01]`

### 👀 Demo — what works after this merges
A user opening `http://localhost:3000/login` with an active password manager browser extension (like Surfshark) sees the authentication form load seamlessly without any red `Hydration failed` runtime errors or console warnings.

### 🛠️ Implementation Tasks
- [x] Add client `mounted` lifecycle guard (`useEffect`) inside `src/components/features/auth-form.tsx`.
- [x] Render a static visual card skeleton during server-side pre-render so extensions have no raw `<input>` tags to mutate prior to hydration.
- [x] Render interactive inputs only after hydration completes.
- [x] Verify `frontend-audit` passes with 0 warnings.
- [x] Update feature documentation in `docs/features/auth.md`.

### 🔒 Security in the Definition of Done
- [x] Inputs remain strictly validated at boundary via `SignUpInputSchema` and `SignInInputSchema`.
- [x] No sensitive passwords or credentials rendered into static SSR placeholder HTML.
- [x] Design system token compliance (`DESIGN.md`) and WCAG-AA contrast verified (0 warnings).
- [x] No hardcoded secrets.

### 🧪 Verification Plan
- **Automated verification:** `python C:\Users\kishore\.gemini\config\skills\frontend-audit\audit.py DESIGN.md src/components/features/auth-form.tsx`
- **Manual verification:** Load `http://localhost:3000/login` in Google Chrome / Edge with Surfshark extension active; confirm browser console contains 0 hydration errors.
