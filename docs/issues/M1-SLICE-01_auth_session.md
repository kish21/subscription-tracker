---
name: Feature Implementation Ticket
about: One scope-bounded engineering ticket generated by /tickets (Plan -> Contracts -> Ticket)
title: "[M1-SLICE-01] Authentication & Session Lifecycle"
labels: ["enhancement", "product-playbook", "milestone-1"]
---

### 🎯 Goal
Implement secure, self-hosted authentication (signup, login, logout) with PostgreSQL persistence, httpOnly cookie session management, and fail-closed auth route guards.

### 🧭 Slice Strategy
- [x] ↕️ Vertical — thin end-to-end increment, demoable on merge
- [ ] ↔️ Horizontal — single architectural layer
- [ ] 🐛 Ad-hoc — bug · edge-case · tech-debt · security

### 🛣️ Lane
auth

### 🧱 Layer(s)
- [x] 📦 Storage / data provider — schema models, persistence adapter, migration
- [x] ⚙️ Pure domain service — business logic, state transitions, validation
- [x] 🎨 UI component — isolated component, interactive states, a11y, `DESIGN.md` tokens
- [x] 🧪 Cross-layer verification — integration across the real seam, adversarial/security

### 📁 Target Files
- `src/auth/server.ts`
- `src/auth/client.ts`
- `src/auth/session.ts`
- `src/app/api/auth/[...all]/route.ts`
- `src/app/(auth)/login/page.tsx`
- `src/app/(auth)/signup/page.tsx`
- `src/components/features/auth-form.tsx`
- `tests/integration/auth.test.ts`
- `docs/features/auth.md`

### 🔌 Contract — Inputs → Outputs
- **Consumes:** `SignUpInputSchema`, `SignInInputSchema` (`src/schemas/auth.ts`)
- **Exposes:** `AuthSessionResponseSchema` (`src/schemas/auth.ts`), httpOnly session cookie (`session_token`), `getCurrentUser()` helper

### 🔗 Depends On
None (builds on M0 foundation and contracts)

### 👀 Demo — what works after this merges
Reviewer can navigate to `/signup`, register a user with email, password, and account currency (`USD`, `EUR`, etc.), get redirected to `/dashboard`, observe the secure session cookie in browser devtools, log out, and log back in at `/login`. Unauthenticated requests to protected pages redirect to `/login`.

### 🛠️ Implementation Tasks
- [x] Configure Better Auth in `src/auth/server.ts` using Drizzle adapter with existing `user`, `session`, `account`, `verification` tables.
- [x] Implement Next.js catch-all route handler `src/app/api/auth/[...all]/route.ts`.
- [x] Implement `src/auth/session.ts` with fail-closed `getCurrentUser(req)` adhering to ADR-002.
- [x] Create accessible Auth Form component `src/components/features/auth-form.tsx` obeying `DESIGN.md` tokens and `/new-component` rules.
- [x] Create `/signup` and `/login` pages in `src/app/(auth)/`.
- [x] Write integration test `tests/integration/auth.test.ts` verifying password hashing, session issuance, invalid credentials rejection, and currency persistence.
- [x] Write feature documentation in `docs/features/auth.md`.

### 🔒 Security in the Definition of Done
- [x] Inputs validated against typed Zod schema (`SignUpInputSchema`, `SignInInputSchema`) at boundary.
- [x] Passwords hashed with modern KDF (Argon2id/bcrypt) via Better Auth; never stored or logged in plain text.
- [x] Session cookie marked `httpOnly`, `SameSite=Lax`, and `Secure` in production.
- [x] Fail-closed authentication guards (unauthenticated requests rejected with 401 or redirected).
- [x] No secrets in source code; `BETTER_AUTH_SECRET` read exclusively via typed config loader.
- [x] Integration test verifying invalid credentials cannot obtain sessions.

### 🧪 Verification Plan
- **Automated tests:** `pnpm test tests/integration/auth.test.ts`
- **Manual demo path:** Visit `http://localhost:3000/signup`, register `test@example.com`, check cookie in storage, log out, log back in at `/login`.
