---
name: Feature Implementation Ticket
about: One scope-bounded engineering ticket generated by /tickets (Plan -> Contracts -> Ticket)
title: "[M2-SLICE-02] Category Filtering & Spend Breakdown"
labels: ["enhancement", "product-playbook", "milestone-2"]
---

### 🎯 Goal
Implement category filtering for the subscriptions list and render a breakdown of spending by category (both minor units amount and percentage of total monthly spend).

### 🧭 Slice Strategy
- [x] ↕️ Vertical — thin end-to-end increment, demoable on merge
- [ ] ↔️ Horizontal — single architectural layer
- [ ] 🐛 Ad-hoc — bug · edge-case · tech-debt · security

### 🛣️ Lane
category-breakdown

### 🧱 Layer(s)
- [ ] 📦 Storage / data provider — schema models, persistence adapter, migration
- [x] ⚙️ Pure domain service — business logic, state transitions, validation
- [x] 🎨 UI component — isolated component, interactive states, a11y, `DESIGN.md` tokens
- [x] 🧪 Cross-layer verification — integration across the real seam, adversarial/security

### 📁 Target Files
- `src/components/features/category-spend-chart.tsx`
- `src/components/features/category-filter-bar.tsx`
- `tests/unit/category-breakdown.test.ts`
- `tests/integration/category-filter.test.ts`
- `docs/features/category-breakdown.md`

### 🔌 Contract — Inputs → Outputs
- **Consumes:** `CategorySpendBreakdownSchema` (`src/schemas/dashboard.ts`), `category` query param
- **Exposes:** Filtered table view, category breakdown statistics

### 🔗 Depends On
`[M1-SLICE-03]`

### 👀 Demo — what works after this merges
User sees category pills above the subscriptions table (All, Entertainment, Software, Utilities, etc.). Clicking a pill filters the table in place. A category spend breakdown component shows each category's monthly sum and percentage of total spend. Category percentages sum to 100.0%.

### 🛠️ Implementation Tasks
- [ ] Implement category spend aggregation in domain logic (`src/domain/calculations.ts`).
- [ ] Build `CategoryFilterBar` component (`src/components/features/category-filter-bar.tsx`) with accessible pill buttons.
- [ ] Build `CategorySpendChart` component (`src/components/features/category-spend-chart.tsx`) with progress/bar indicators adhering to `DESIGN.md` tokens.
- [ ] Integrate category filter with `SubscriptionTable` via URL query param or client state.
- [ ] Write unit tests `tests/unit/category-breakdown.test.ts` asserting correct grouping and percentage rounding.
- [ ] Write integration tests `tests/integration/category-filter.test.ts` verifying filtering behavior.
- [ ] Write feature documentation in `docs/features/category-breakdown.md`.

### 🔒 Security in the Definition of Done
- [x] Category values restricted to allowed enum values in `product.yaml`.
- [x] Division by zero guarded when total monthly spend is 0.
- [x] Percentage values strictly clamped between 0.0 and 100.0.
- [x] `/frontend-audit` verifies all new interactive pills and bars adhere to WCAG-AA contrast and focus states.

### 🧪 Verification Plan
- **Automated tests:** `pnpm test tests/unit/category-breakdown.test.ts tests/integration/category-filter.test.ts`
- **Manual demo path:** Add subscriptions across 3 categories, verify each category percentage and sum matches total, click filter pill to see only matching subscriptions.
