---
name: Feature Implementation Ticket
about: One scope-bounded engineering ticket generated by /tickets (Plan -> Contracts -> Ticket)
title: "[M1-SLICE-02] Subscription Domain & Storage Seam"
labels: ["enhancement", "product-playbook", "milestone-1"]
---

### 🎯 Goal
Implement the subscription persistence repository with mandatory tenant-isolation (`userId`), domain arithmetic (integer minor units, billing cycle normalisation), and REST API endpoints for subscription creation and listing.

### 🧭 Slice Strategy
- [x] ↕️ Vertical — thin end-to-end increment, demoable on merge
- [ ] ↔️ Horizontal — single architectural layer
- [ ] 🐛 Ad-hoc — bug · edge-case · tech-debt · security

### 🛣️ Lane
subscriptions

### 🧱 Layer(s)
- [x] 📦 Storage / data provider — schema models, persistence adapter, migration
- [x] ⚙️ Pure domain service — business logic, state transitions, validation
- [ ] 🎨 UI component — isolated component, interactive states, a11y, `DESIGN.md` tokens
- [x] 🧪 Cross-layer verification — integration across the real seam, adversarial/security

### 📁 Target Files
- `src/domain/calculations.ts`
- `src/db/repositories/subscription.ts`
- `src/app/api/v1/subscriptions/route.ts`
- `tests/unit/calculations.test.ts`
- `tests/integration/subscriptions.test.ts`
- `docs/features/subscriptions-crud.md`

### 🔌 Contract — Inputs → Outputs
- **Consumes:** `CreateSubscriptionInputSchema`, `SubscriptionFilterSchema` (`src/schemas/subscription.ts`)
- **Exposes:** `SubscriptionResponseSchema`, `SubscriptionListResponseSchema` (`src/schemas/subscription.ts`), `SubscriptionRepository` interface

### 🔗 Depends On
`[M1-SLICE-01]` (depends on authenticated session reader for `userId`)

### 👀 Demo — what works after this merges
An authenticated user can submit a POST request to `/api/v1/subscriptions` with name, price in minor units or decimal, billing cycle, category, and renewal date, and fetch their subscription list via GET `/api/v1/subscriptions`. Cross-account tests prove User B cannot read or list User A's subscriptions.

### 🛠️ Implementation Tasks
- [ ] Implement pure domain arithmetic in `src/domain/calculations.ts`: cycle normalisation (monthly/quarterly/semiannual/yearly to monthly equivalent cents), upcoming renewal filtering (within N days window).
- [ ] Implement `SubscriptionRepository` in `src/db/repositories/subscription.ts` requiring `userId` on every query (ADR-005).
- [ ] Implement `GET` and `POST` handlers in `src/app/api/v1/subscriptions/route.ts` with session authentication and Zod schema validation.
- [ ] Write unit tests for domain calculations in `tests/unit/calculations.test.ts` verifying rounding, integer minor units, and date comparisons.
- [ ] Write integration tests in `tests/integration/subscriptions.test.ts` verifying tenant isolation, data persistence, and idempotency key handling.
- [ ] Write feature documentation in `docs/features/subscriptions-crud.md`.

### 🔒 Security in the Definition of Done
- [x] Every repository query takes `userId`; no query path exists without tenant scoping (ADR-005).
- [x] Money stored and calculated strictly as integer minor units (`priceMinorUnits`, integer >= 0, ADR-004); floating point money strictly rejected.
- [x] Input validation against `CreateSubscriptionInputSchema`; category and billing cycle validated against closed sets.
- [x] Handlers fail closed (401 on missing session, 400 on malformed body).
- [x] Subscription names and financial prices are excluded from logs.
- [x] Adversarial integration test proves User B receives 0 rows when requesting User A's subscriptions.

### 🧪 Verification Plan
- **Automated tests:** `pnpm test tests/unit/calculations.test.ts tests/integration/subscriptions.test.ts`
- **Manual demo path:** Execute `curl -X POST http://localhost:3000/api/v1/subscriptions` with authenticated session cookie and inspect JSON response.
